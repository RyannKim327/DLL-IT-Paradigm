import { useState } from 'react'

export default function Events(){
  // TODO: To create a Event section where it could be posted here.
  const [ _event, setEvent ] = useState()
  document.title = "Events in IT Paradigm"
  return(
    <div>
      <p>tests</p>
    </div>
  )
}
